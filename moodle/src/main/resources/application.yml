management:
  endpoints.web.exposure.include: '*'
  metrics:
    export:
      prometheus:
        enabled: true
    distribution:
      percentiles-histogram:
        "[http.server.requests]": true
spring:
  jmx:
    enabled: true
    default-domain: myapp


